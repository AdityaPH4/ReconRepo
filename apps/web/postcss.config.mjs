/**
 * Tailwind v4 needs no JS config — design tokens and component classes live in
 * `app/globals.css` instead. This file only registers the PostCSS plugin.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
