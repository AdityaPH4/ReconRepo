{
  "name": "@toit/mpr-core",
  "version": "1.0.0",
  "private": true,
  "description": "Pure MPR (Layer 2) reconciliation engine — bank-file adapters, matcher, CSV export. No DOM, no I/O, no database.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "tsc -b && node --test \"test/*.test.ts\""
  },
  "dependencies": {
    "@toit/recon-core": "*",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
